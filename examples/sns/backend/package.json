{
    "name": "example-sns-backend",
    "private": true,
    "scripts": {
        "dev": "vite --configLoader runner",
        "typecheck": "tsc --noEmit",
        "test": "pnpm vitest run",
        "e2e": "pnpm vitest run src/server.test.ts",
        "build": "vite build --configLoader runner --ssr src/server.ts"
    },
    "dependencies": {
        "@hono/node-server": "^1.19.9",
        "hono": "^4.12.0"
    },
    "devDependencies": {
        "@hono/vite-dev-server": "^0.25.0",
        "typescript": "^5.9.3",
        "vite": "^7.3.1",
        "vitest": "^4.0.18"
    }
}
